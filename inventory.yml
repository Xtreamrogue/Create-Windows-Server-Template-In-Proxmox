---
proxmox:
  hosts:
    192.168.80.254:  #Proxmox IP Address
  vars:
    ansible_user: root  # Proxmox User
    ansible_password: 'Proxmox Password' # Proxmox user password
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    ansible_python_interpreter: auto_silent
